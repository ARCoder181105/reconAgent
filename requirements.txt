# ReconAgent — Python dependencies (backend + matcher)
# Python 3.12.3  (.python-version)
# Installed into .venv

# --- Matching engine ---
pandas>=2.2,<4
rapidfuzz>=3.14
numpy>=2.0

# --- API / backend ---
fastapi>=0.141
uvicorn[standard]>=0.30
sqlalchemy>=2.0
pydantic>=2.13

# --- LLM (Stage 5 only) ---
google-genai>=2.0

# --- Dev / testing ---
pytest>=8.0
httpx>=0.27
