"""Shared utility functions reused across backend folders.

Kept free of domain imports so any package (matcher, data_generator, eval,
routers, tests) can depend on them without circular imports.
"""
